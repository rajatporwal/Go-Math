import {
  ARRAY_HELPER,
  STRING_HELPER,
  OBJECT_HELPER,
  MAP_HELPER,
  SET_HELPER
} from "../helper";

const dataStructureConfig = [
  ARRAY_HELPER,
  STRING_HELPER,
  OBJECT_HELPER,
  MAP_HELPER,
  SET_HELPER
];

export default dataStructureConfig;
