import {
  ARRAY_HELPER,
  STRING_HELPER,
  OBJECT_HELPER,
  MAP_HELPER
} from "../utils";

const dataStructureConfig = [
  ARRAY_HELPER,
  STRING_HELPER,
  OBJECT_HELPER,
  MAP_HELPER
];

export default dataStructureConfig;
